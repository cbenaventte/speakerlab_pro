# Renombra este archivo a .env y rellena los valores reales
# NUNCA subas .env a Git

# ── Stripe ─────────────────────────────────────────────────────
STRIPE_SECRET_KEY=sk_test_REEMPLAZA_CON_TU_CLAVE
STRIPE_PUBLISHABLE_KEY=pk_test_REEMPLAZA_CON_TU_CLAVE
STRIPE_WEBHOOK_SECRET=whsec_REEMPLAZA_CON_TU_WEBHOOK_SECRET

# ── PayPal ─────────────────────────────────────────────────────
PAYPAL_CLIENT_ID=REEMPLAZA
PAYPAL_CLIENT_SECRET=REEMPLAZA
PAYPAL_MODE=sandbox        # cambiar a "live" en producción

# ── App ────────────────────────────────────────────────────────
PDF_PRICE_USD=10.00
TOKEN_SECRET=genera-con-python-secrets.token_hex(32)

# ── Opcional: Redis para tokens de pago (en vez de dict en memoria)
# REDIS_URL=redis://localhost:6379
